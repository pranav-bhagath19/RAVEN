# RAVEN Policy Engine Specification

## 1. Absolute Policy Veto Authority

The **Deterministic Policy Engine** is the non-bypassable governance layer of RAVEN. 

### Fundamental Rule
An AI Agent (Root Cause Analyst, Recovery Planner, or Verification Agent) can **never** execute an action directly or override a policy rule. 

Every candidate action generated by an AI Agent must be submitted to the Policy Engine for evaluation. The Policy Engine evaluates the action against strict deterministic business rules and issues one of three non-negotiable decisions:
1. `APPROVED`: Generates an ephemeral cryptographic `PolicyApprovalToken` granting permission for side-effect tool execution.
2. `BLOCKED`: Rejects the action completely, records a policy block entry in `DecisionTrace`, and halts execution.
3. `ESCALATE_TO_HUMAN`: Routes the transaction and proposed candidate action to the merchant operations dashboard for manual review.

---

## 2. Core Policy Definitions

### Policy Rule 1: Captured Payment Guard (`POL_001`)
- **Rule**: If `payment.status == CAPTURED` or `order.status == PAID`, **ALL** candidate recovery actions MUST BE BLOCKED immediately.
- **Decision**: `BLOCKED`

### Policy Rule 2: Ambiguous State Isolation (`POL_002`)
- **Rule**: If `payment.status == AMBIGUOUS` or `payment.status == PENDING`, automated retry or communication actions are prohibited until state verification resolves status.
- **Decision**: `ESCALATE_TO_HUMAN`

### Policy Rule 3: Maximum Recovery Attempt Cap (`POL_003`)
- **Rule**: A single `OpportunityID` or `PaymentID` may not exceed `max_recovery_attempts` (default: 3 attempts per 72 hours).
- **Decision**: `BLOCKED` if `attempts_count >= max_attempts`.

### Policy Rule 4: High-Value Transaction Boundary (`POL_004`)
- **Rule**: If transaction amount exceeds `high_value_threshold` (e.g. ₹10,000 in minor units), autonomous execution is disabled.
- **Decision**: `ESCALATE_TO_HUMAN`

### Policy Rule 5: Low Confidence Threshold (`POL_005`)
- **Rule**: If candidate action `agent_confidence < min_confidence_threshold` (default: `0.75`), autonomous tool dispatch is blocked.
- **Decision**: `ESCALATE_TO_HUMAN` or `BLOCKED`.

### Policy Rule 6: Customer Opt-Out & Communication Cap (`POL_006`)
- **Rule**: If customer has opted out of messaging or daily message cap is reached, external messaging tools MUST BE BLOCKED.
- **Decision**: `BLOCKED`

### Policy Rule 7: Systemic Bank Downtime Pause (`POL_007`)
- **Rule**: If failure patterns report bank downtime failure rate exceeding `40%`, automated retries targeting that bank are paused.
- **Decision**: `BLOCKED` (retry scheduled post-downtime).

---

## 3. Decision Trace Integration

Policy evaluation results are recorded in the `policy_evaluations` field of the [`DecisionTrace`](file:///c:/Users/prana/Documents/RAVEN/docs/decision-trace.md) entity.
